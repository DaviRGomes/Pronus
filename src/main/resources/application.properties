# --- BANCO DE DADOS H2 ---
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# --- JPA / HIBERNATE (Cria as tabelas sozinho) ---
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- APIS EXTERNAS ---
gemini.api.key=AIzaSyCYX8AB-xmGu80-wOHSgy6DyyPsjnbpfgA
deepgram.api.key=21116d4bbe100599ced4cda751a5696dee554940

# --- SEGURANÇA ---
api.security.token.secret=${JWT_SECRET:minha-chave-secreta-super-segura-123}
api.security.token.expiration=86400000

# --- UPLOAD ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# --- DESATIVAR FLYWAY EXPLICITAMENTE (Por segurança) ---
spring.flyway.enabled=false
